import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
   
    mensagem: {
        fontSize: 25,
        margin: 10,
        color: '4d469e' 
    },

    vimensagem: {
        borderWidth: 2 ,
        borderColor: '#36205c',
        borderRadius: 20,
        marginBottom: 10,
    },

});

export default styles;
